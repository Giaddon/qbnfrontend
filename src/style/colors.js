
const neutral = "#ffffff"; // Default: "#ffffff"

const informative = "#66bfbf"; // Default: "#66bfbf", used for tooltips and outcomes.
const informativeAccent = "#056674"; // Default: "#056674";

const forbidden = "#ff6347"; // Default: "#ff6347", used for locked actions

const highlight = "#ffd700" // Default: "#ffd700", used to highlight (i.e., hovering over an action)

const green = "#66bfbf";
const darkgreen = "#056674";

const navbar = "#005086"; // Default: "#005086", used for the navbar
const navbarAccent = "#318fb5" // Default: "#318fb5" used for navbar accent (i.e. the buttons)

const text = "#000" // Default: "#000", used for most text

export {
  neutral, 
  informative, 
  informativeAccent, 
  forbidden,
  highlight, 
  green, 
  darkgreen, 
  text,
  navbar,
  navbarAccent,
};